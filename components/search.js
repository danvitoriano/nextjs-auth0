import React from "react";

const Search = (props) => {
    console.log(props)
    return (
        <>
            
        </>
    );
};

export default (props) => {
    return (
        <div>
            <Search {...props} />
        </div>
    );
};
